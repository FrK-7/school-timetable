package com.school.timetable.repository;

import com.school.timetable.model.Plesso;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlessoRepository extends JpaRepository<Plesso, Long> {
}
